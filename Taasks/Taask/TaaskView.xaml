<UserControl
    x:Class="Taasks.TaaskView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Taasks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Margin="3"
    HorizontalAlignment="Center"
    VerticalAlignment="Center"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Background="Transparent"
    mc:Ignorable="d">
    <Border>
        <StackPanel>
            <TextBlock x:Name="Title" Style="{StaticResource TextStyle}" />
            <TextBlock x:Name="Time" Style="{StaticResource TextStyle}" />
            <Button x:Name="Toggle" Content="{Binding Label}" />
        </StackPanel>
    </Border>
</UserControl>
